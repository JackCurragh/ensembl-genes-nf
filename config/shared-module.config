// shared-module.config — process-level overrides shared across all sub-pipelines.
// Add withName selectors here for any process that needs non-default ext.args,
// retry logic, or conditional execution.
process {
}